@model SistemaSpaWeb.Models.Membresia

<div class="container mt-4">
    <div class="card shadow border-danger">
        <div class="card-header bg-danger text-white">
            <h5 class="mb-0">
                <i class="bi bi-trash"></i> Eliminar Membresía
            </h5>
        </div>

        <div class="card-body">
            <div class="alert alert-warning">
                ¿Está seguro de eliminar esta membresía?
            </div>

            <ul class="list-group mb-3">
                <li class="list-group-item"><strong>Nombre:</strong> @Model.NombreMembresia</li>
                <li class="list-group-item"><strong>Precio:</strong> $@Model.Precio</li>
                <li class="list-group-item"><strong>Estado:</strong> @Model.Estado</li>
            </ul>

            <form asp-action="Delete">
                <input type="hidden" asp-for="MembresiaID" />

                <div class="text-end">
                    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Eliminar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
